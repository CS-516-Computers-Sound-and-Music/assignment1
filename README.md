# assignment1

Briefly, I used `scipy.io.wavfile` to read/write a normal and a clipped sin wave to a .wav file. I used `sounddevice` to output this audio directly to my computer's speakers. As the directions specify, I used the parameters ... 
- amplitude = 8192 (half )
- frequency = 400
- sample_rate = 48000

